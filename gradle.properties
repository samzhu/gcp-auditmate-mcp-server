# Enable Gradle Daemon (recommended to disable in CI environments)
org.gradle.daemon=false
# Enable parallel build (improves build speed on multi-core machines)
org.gradle.parallel=true
# Enable build cache (improves build speed if cache directory is configured)
org.gradle.caching=true